version: "3.7"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api-product
    env_file:
        - .env
    volumes: 
      - ".:/app"
    ports:
      - "8001:8000"
    tty: true